'use strict';

module.exports = (function () {
    return {
        init: function(element, nodes, links, options){
            console.log(element);
            console.log(nodes);
            console.log(links);
            console.log(options);
        }
    }
})();